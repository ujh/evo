name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: true
    - uses: jdx/mise-action@v4
    - name: Setup
      run: mise run setup
    - name: Test
      run: mise run test
